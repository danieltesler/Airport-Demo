import { ChatWindow } from "@/components/ChatWindow";

/** Single-page chat app. All interactivity lives in ChatWindow (a client component). */
export default function HomePage() {
  return <ChatWindow />;
}
